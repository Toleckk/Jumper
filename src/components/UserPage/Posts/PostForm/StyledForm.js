import styled from "styled-components";
import {Form} from "components/Common/molecules";
import {Flex} from "components/Common/atoms";

export default styled(Flex).attrs({as: Form, direction: 'column', align: 'flex-end'})``;